import { MouseHoverStyleDirective } from './mouse-hover-style.directive';

describe('MouseHoverStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseHoverStyleDirective();
    expect(directive).toBeTruthy();
  });
});
